name: C/C++ CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]


jobs:

  unit_tests:
      name: unit_tests
      runs-on: ubuntu-latest

      steps:
      - name: Intro
        run: echo "Starting unit tests"
        
      #- name: unit_testing_step
      #  uses: actions/unit_testing_step@v1
      
      - name: Outro
        run: echo "Unit testing successful"
